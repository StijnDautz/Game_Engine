#pragma once
#include <vector>
#include <iostream>
#include "Sprite.h"

class ImageLoader
{
	/*
public:
	ImageLoader();

	Sprite LoadImageAsBuffer(char* filename);

private:
	void loadFile(std::vector<unsigned char>& buffer, const std::string& filename);*/
};